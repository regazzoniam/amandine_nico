{% extends 'base-admin.html.twig' %}

{% block body%}
    <div class="container text-white">
        <h2 class="text-green text-title">Ajouter un Forum</h2>
        {{ form_start(formForum)}}
        {{ form_end(formForum)}}
    </div>
{% endblock %}