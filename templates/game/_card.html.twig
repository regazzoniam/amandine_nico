<div class="col-lg-4 col-md-6 card border-lightgrey bg-lightgrey">
    {% if game.thumbnailCover is defined %}
        <a class="text-decoration-none" href="{{ path('app_game_show', {'slug': game.slug}) }}">
            <div class="image-container">
                <img src="{{game.thumbnailCover}}" class="card-img-top mx-auto text-white" alt="{{game.name}}">
            </div>
        </a>
    {% endif %}
    <div class="card-body">
    <div class="card-text row">
        <p class="text-white text-title col-8">{{game.name}}</p>
        <p class="text-green text-title col-4">{{game.price}}€</p>
    </div>
    </div>
</div>